Close VS Code / all terminals
Fresh PowerShell → cd C:\GitHub\parasolpy → poetry env remove --all
If still stubborn, nuke the folder manually with Remove-Item
poetry install to create the in-project .venv
poetry add pandas plotly dash numpy matplotlib scipy seaborn hiplot platypus-opt scikit-learn beautifulsoup4
Build, publish, claim the name