# Minimal Dash App (Notebook)

This folder is the notebook version of `example_scripts/minimal_dash_app.py`.

## Structure
- `data/`: placeholder for any local data you may want to test with
- `output/`: optional folder for exported app outputs or screenshots
- `minimal_dash_app.ipynb`: the converted example notebook

## Notes
- The notebook currently uses Plotly's public Gapminder CSV URL, matching the original script behavior.
- Run the final cell to start the Dash server.
