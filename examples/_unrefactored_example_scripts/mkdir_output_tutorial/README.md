# mkdir Output Tutorial (Notebook)

This folder is the notebook version of `example_scripts/mkdir_output_tutorial.py`.

## Structure
- `data/`: contextual RiverWare/Borg model files copied from `problems/MinimalExample/`
- `output/`: generated outputs from the notebook
- `mkdir_output_tutorial.ipynb`: the converted example notebook

## Notes
- The notebook creates `output/` if needed and writes `output/sample_results.csv`.
